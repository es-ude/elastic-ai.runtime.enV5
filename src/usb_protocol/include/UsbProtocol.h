#ifndef ENV5_USB_PROTOCOL_HEADER
#define ENV5_USB_PROTOCOL_HEADER

/*
 * Required Functions:
 * - Default Commands
 *   -> See section "Commands" of issue #244
 * - Register Function
 *   -> check if index over 128
 * - Wait For Command
 * - Setup Protocol
 *   -> add default functions to function pointer array
 *   -> setup input/output methods
 */

#endif // ENV5_USB_PROTOCOL_HEADER
